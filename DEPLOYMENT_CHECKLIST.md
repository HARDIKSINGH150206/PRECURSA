#!/bin/bash
# Deployment Checklist for Precursa

echo "🚀 Precursa Deployment Checklist"
echo "=================================="
echo ""

echo "STEP 1: Prepare Your Repositories"
echo "---------------------------------"
echo "[ ] Push code to GitHub"
echo "[ ] Ensure main branch is up-to-date"
echo "[ ] Create .env files locally (use .env.example as template)"
echo ""

echo "STEP 2: Backend Setup (Render)"
echo "------------------------------"
echo "[ ] Create Render account at https://render.com"
echo "[ ] Create PostgreSQL database:"
echo "    - Go to New → PostgreSQL"
echo "    - Name it 'precursa-db'"
echo "    - Save connection string"
echo "[ ] Create Web Service:"
echo "    - New → Web Service"
echo "    - Connect GitHub repo"
echo "    - Settings:"
echo "      - Runtime: Python 3"
echo "      - Build: pip install -r backend/requirements.txt"
echo "      - Start: uvicorn backend.app.main:app --host 0.0.0.0 --port \$PORT"
echo "[ ] Add Environment Variables in Render:"
echo "    - DATABASE_URL (from PostgreSQL)"
echo "    - FRONTEND_ORIGIN (temp: http://localhost:5174)"
echo "    - All API keys (AIS, Gemini, Weather, News)"
echo "    - Clerk issuer and IDs"
echo "[ ] Deploy and note the backend URL (e.g., https://precursa-backend.onrender.com)"
echo ""

echo "STEP 3: Frontend Setup (Vercel)"
echo "-------------------------------"
echo "[ ] Create Vercel account at https://vercel.com"
echo "[ ] Import GitHub repository"
echo "[ ] Project Settings:"
echo "    - Framework: Vite"
echo "    - Root Directory: frontend"
echo "    - Build: npm run build"
echo "    - Output: dist"
echo "[ ] Add Environment Variables:"
echo "    - VITE_API_BASE_URL=https://precursa-backend.onrender.com"
echo "    - VITE_CLERK_PUBLISHABLE_KEY=your_key"
echo "[ ] Deploy and note the frontend URL (e.g., https://precursa.vercel.app)"
echo ""

echo "STEP 4: Final Configuration"
echo "---------------------------"
echo "[ ] Update Render backend FRONTEND_ORIGIN to Vercel URL"
echo "[ ] Test backend at: https://precursa-backend.onrender.com/docs"
echo "[ ] Test frontend at: https://precursa.vercel.app"
echo "[ ] Log in with Clerk"
echo "[ ] Make API calls to verify backend connectivity"
echo ""

echo "STEP 5: Production Verification"
echo "--------------------------------"
echo "[ ] Check Render logs for errors"
echo "[ ] Check Vercel build logs for errors"
echo "[ ] Test full user flow (login → view data → make API calls)"
echo "[ ] Verify database queries work"
echo "[ ] Test all API endpoints"
echo ""

echo "✅ Deployment Complete!"
